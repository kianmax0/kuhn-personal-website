# Kuhn Personal Site

This is a minimal React + Vite personal site scaffold.

Run locally:

```bash
cd /Users/kuhn/Desktop/Web_Development/kuhn
npm install
npm run dev
```

Routes:

- `/` Home
- `/moments` Moments (like a simple social feed)
